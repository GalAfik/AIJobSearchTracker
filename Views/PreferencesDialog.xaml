<Window x:Class="JobSearchTracker.Views.PreferencesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Preferences" Height="350" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Appearance" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                
                <TextBlock Text="Theme:"/>
                <ComboBox x:Name="ThemeComboBox" SelectionChanged="ThemeComboBox_SelectionChanged">
                    <ComboBoxItem Content="Light" Tag="Light"/>
                    <ComboBoxItem Content="Dark" Tag="Dark"/>
                </ComboBox>

                <Separator Margin="0,10"/>

                <TextBlock Text="Directions" FontSize="16" FontWeight="Bold" Margin="0,10,0,10"/>
                
                <TextBlock Text="Home Address:"/>
                <TextBlock FontWeight="Normal" TextWrapping="Wrap" Margin="0,0,0,5" FontSize="11" Foreground="Gray">
                    This address will be used as the starting point when getting directions to job locations.
                </TextBlock>
                <TextBox x:Name="HomeAddressTextBox" 
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         Height="80"
                         VerticalScrollBarVisibility="Auto"/>

                <Separator Margin="0,10"/>

                <TextBlock Text="Default Sorting" FontSize="16" FontWeight="Bold" Margin="0,10,0,10"/>
                
                <TextBlock Text="Sort Jobs By:"/>
                <ComboBox x:Name="SortByComboBox">
                    <ComboBoxItem Content="Date Added (Newest)"/>
                    <ComboBoxItem Content="Date Added (Oldest)"/>
                    <ComboBoxItem Content="Company Name (A-Z)"/>
                    <ComboBoxItem Content="Company Name (Z-A)"/>
                    <ComboBoxItem Content="Date Applied (Newest)"/>
                    <ComboBoxItem Content="Date Applied (Oldest)"/>
                    <ComboBoxItem Content="Status"/>
                </ComboBox>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Width="80" Height="30" Margin="0,0,10,0" Click="SaveButton_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="80" Height="30" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
